pipeline {
 agent any
  stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/sachinshaji/airbus-project.git'
                sh 'ls -latrh'
            }
        }
    }
}
