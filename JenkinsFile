pipeline {
 agent any
  stages {
        stage('Example Build') {
            steps {
                echo "Hello, Maven"
            }
        }
        stage('Checkout') {
            steps {
                git 'https://github.com/sachinshaji/airbus-project.git'
                sh 'ls -latrh'
            }
        }
    }
}
