pipeline {
 agent any
  stages {
        stage('Checkout') {
            steps {
                sh git clone 'https://github.com/sachinshaji/airbus-project.git'
                sh 'ls -latrh'
            }
        }
    }
}
