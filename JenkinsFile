pipeline {
 agent any
  stages {
        stage('Example Build') {
            steps {
                echo "Hello, Maven"
            }
        }
        stage('Checkout') {
            steps {
                git 'https://github.com/jenkinsci/git-plugin'
                sh 'ls -latrh'
            }
        }
    }
}
